# Migration `20210105021410-change-id`

This migration has been generated by kalwabed at 1/5/2021, 9:14:10 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Link" (
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "description" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "postedById" TEXT NOT NULL,

    FOREIGN KEY ("postedById") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Link" ("id", "createdAt", "description", "url", "postedById") SELECT "id", "createdAt", "description", "url", "postedById" FROM "Link";
DROP TABLE "Link";
ALTER TABLE "new_Link" RENAME TO "Link";
CREATE TABLE "new_User" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    PRIMARY KEY ("id")
);
INSERT INTO "new_User" ("id", "name", "email", "password") SELECT "id", "name", "email", "password" FROM "User";
DROP TABLE "User";
ALTER TABLE "new_User" RENAME TO "User";
CREATE UNIQUE INDEX "User.email_unique" ON "User"("email");
CREATE TABLE "new_Vote" (
    "id" TEXT NOT NULL,
    "linkId" TEXT NOT NULL,
    "userId" TEXT NOT NULL,

    FOREIGN KEY ("linkId") REFERENCES "Link"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Vote" ("id", "linkId", "userId") SELECT "id", "linkId", "userId" FROM "Vote";
DROP TABLE "Vote";
ALTER TABLE "new_Vote" RENAME TO "Vote";
CREATE UNIQUE INDEX "Vote.linkId_userId_unique" ON "Vote"("linkId", "userId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201125185150-init..20210105021410-change-id
--- datamodel.dml
+++ datamodel.dml
@@ -1,37 +1,37 @@
 datasource db {
-  provider = "sqlite" 
-  url = "***"
+  provider = "sqlite"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Link {
-  id          Int      @id @default(autoincrement())
+  id          String   @id @default(cuid())
   createdAt   DateTime @default(now())
   description String
   url         String
   postedBy    User     @relation(fields: [postedById], references: [id])
-  postedById  Int
+  postedById  String
   votes       Vote[]
 }
 model User {
-  id       Int    @id @default(autoincrement())
+  id       String @id @default(cuid())
   name     String
   email    String @unique
   password String
   links    Link[]
   votes    Vote[]
 }
 model Vote {
-  id     Int  @id @default(autoincrement())
-  link   Link @relation(fields: [linkId], references: [id])
-  linkId Int
-  user   User @relation(fields: [userId], references: [id])
-  userId Int
+  id     String @id @default(cuid())
+  link   Link   @relation(fields: [linkId], references: [id])
+  linkId String
+  user   User   @relation(fields: [userId], references: [id])
+  userId String
   @@unique([linkId, userId])
-}
+}
```


